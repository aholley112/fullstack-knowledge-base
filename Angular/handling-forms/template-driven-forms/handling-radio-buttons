# Handling radio buttons

# Example

<div class="radio" *ngFor="let gender of genders">
<label>
<input
type="radio
name="gender"
ngModel
[value]="gender">
{{ gender }}
</label>

We can also use one way binding to set a default.