# Introduction

We have two places where we need to add forms, when we create a new recipe or edit a recipe in the shoppingn list.

We can already add elements but we can do it in a better way through a form. 

