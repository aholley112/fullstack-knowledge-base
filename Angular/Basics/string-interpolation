# String interpolation

Add it to the template with {{ }}
You can also manually put strings inside {{ }}

You can't write block expressions in the curly braces.

It is a one way data binding technique that is used to transfer the data from the TypeScript code to an HTML template(view).